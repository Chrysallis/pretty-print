import { test } from 'qunit';
import moduleForAcceptance from 'prettyprint/tests/helpers/module-for-acceptance';

moduleForAcceptance('Acceptance | display books');

test('should show interactive logo', function (assert) {
});
test('should show every book in collection', function (assert) {
});
test('should open book for editing when clicked on', function (assert) {
});
test('should close book when logo is clicked', function (assert) {
});
